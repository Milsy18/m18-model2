import pandas as pd

def compute_total_score(df: pd.DataFrame) -> pd.Series:
    \"\"\"Stub for total_score: returns zeros until implemented.\"\"\"
    return pd.Series(0, index=df.index, name="total_score")
